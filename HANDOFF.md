# HANDOFF: журнал передачи работы между ИИ

**ChatGPT, начни с этого файла.** Здесь записано, что уже сделано, где что лежит и что делать дальше.
Порядок чтения: `HANDOFF.md` → `AGENTS.md` (правила) → `docs/PLAN.md` (полный план) → `docs/TASKS.md` (задачи).

Правило: закончил кусок работы — добавь запись **в начало** журнала ниже (дата, кто, что сделал, что дальше). Старые записи не удаляй.

---

## 2026-09-25 · Claude · Страница «Как проходит покупка»

- Владелец: «Как проходит покупка — да, делай» (ссылка «Подробнее о процессе →» на главной).
- `how-to-buy.html`: 6 шагов (заявка, подборка, просмотры, проверка и бронь, договор и оплата, ТАПУ и ключи) — у каждого срок, «от вас» / «делаем мы», ссылки на районы, юриста, статью о расходах, ВНЖ; якоря `#step-1…6`.
- Калькулятор расходов сверх цены (`#costs`): цена в выбранной валюте, вторичка / новостройка партнёра, налог ТАПУ пополам, покупка по доверенности; итог в валюте и в % от цены. Расчёт — `KHS.buyCosts` в `assets/site.js`, пригодится для страницы объекта.
- Блоки «Покупка удалённо» (`#remote`), «После покупки», FAQ. JSON-LD HowTo + FAQPage + BreadcrumbList.
- Ссылки: «Подробнее о процессе» на главной, «Подбор и просмотр» и «Проверка документов» в футере всех страниц, блок «Прежде чем покупать» на странице объекта.
- Проверено: 1280, 900, 390 px; калькулятор (150 000 € вторичка = 10 610 €, 7,1%; пополам, новостройка, доверенность; пересчёт при смене валюты).

---

## 2026-09-25 · Claude · Админка «Команда»

- Владелец: «чтобы с админки можно было страницу команды и менеджеров менять».
- `admin/team.html`: список сотрудников (фото, должность, районы, число объектов, статус), порядок стрелками ↑↓, скрыть/показать, профиль на сайте; редактор страницы команды (заголовок, подзаголовок, цитата основателя, 4 цифры; пустая цифра — считается автоматически).
- `admin/member-edit.html?id=`: имя, должность, тип (эксперт / юрист / основатель), опыт, языки, специализация, «о себе», фото (загрузка, сжатие до 600 px), районы, «Чем помогу», отзывы, статус. Проверка полей, «Отменить все правки» для исходных сотрудников, «Удалить» для добавленных.
- Данные: `TEAM_BASE` + правки из `kh_team` (`KH.teamAll`, `KH.saveMember`, `KH.resetMember`); тексты страницы — `kh_teampage` (`KH.teamPage`). На сайте `KH.TEAM` — только видимые, по порядку. `KH.IMG` принимает и загруженные фото (data:).
- Переименование: старое имя сохраняется в `aka`, `KH.byName` находит сотрудника по нему — статьи и объекты не теряют автора и менеджера. Скрытый эксперт: его объекты распределяются по остальным (`KH.agentFor` по `kind === 'expert'`).
- Списки «Ответственный менеджер» (объект) и «Автор» (публикация) строятся из команды.
- Сайт устойчив к скрытию любого сотрудника: команда без основателя, лендинги без юриста, главная берёт первых трёх.
- Исправлено: растянутая аватарка автора в блоге (правило обложки задевало аватарку).
- Проверено в Chromium: полный сценарий «переименовать → скрыть → добавить с фото → порядок → тексты страницы» и отображение на сайте; админка без горизонтальной прокрутки на 1280 и 390 px.

---

## 2026-09-25 · Claude · Страница эксперта

- Владелец выбрал «страница каждого эксперта».
- `member.html?id={founder|expert1|lawyer|expert2|expert3|expert4}`: фото, роль, опыт, языки, районы, WhatsApp / консультация / телефон; «Чем помогу» и «О себе»; районы эксперта (карточки → гиды); объекты, которые он ведёт (как на странице объекта: `o.agent` или `KH.agentFor`; основатель — виллы и премиум, у юриста блока нет); статьи; 2 отзыва (демо); другие эксперты. JSON-LD Person + BreadcrumbList. Неизвестный id — сообщение и ссылка на команду.
- Данные: `help` и `reviews` в `TEAM_MORE` (`data.js`), внесены в `docs/demo-data.md`.
- Ссылки на профиль: имя и кнопка «Профиль» в `team.html`, автор в `post.html`, эксперт на странице объекта, юрист на лендингах ВНЖ и гражданства, команда и «Основатель» на главной, ссылки в текстах новостей.

---

## 2026-09-25 · Claude · Политика конфиденциальности и контакты

- Владелец: «сначала политику конфиденциальности и контакты».
- `privacy.html`: 9 разделов по KVKK (№ 6698) и GDPR — оператор, какие данные, цели и основания (таблица), кому передаём, сроки хранения, права, как отправить запрос, cookies (таблица + настройки, выбор хранится в `kh_consent`), изменения. Содержание сбоку. Текст — черновик для юриста.
- `contacts.html`: телефон, WhatsApp, Telegram, e-mail; офис и шоурум со схемой; эксперты с WhatsApp; реквизиты; FAQ; JSON-LD RealEstateAgent с часами работы.
- Реквизиты (юрлицо, налоговый номер, VERBİS, адрес шоурума) — в `KH.COMPANY`, внесены в `docs/demo-data.md`.
- Ссылки: «политикой обработки персональных данных» во всех формах и «Политика конфиденциальности» в футере → `privacy.html`; «Контакты» в футере и мобильном меню → `contacts.html`.
- Оставшиеся ссылки в никуда только на главной, все записаны в `docs/TASKS.md` (процесс покупки, услуги, отзывы, проданные, партнёрам, вакансии).

---

## 2026-09-25 · Claude · Лендинги «Гражданство» и «ВНЖ»

- Владелец про карточки программ на главной: «тут тоже страницы нужны».
- `citizenship.html` и `residence.html`: главные цифры (порог в $ с пересчётом в выбранную валюту), условия, шаги со сроками, документы, расходы, объекты из каталога (по порогу 400 000 $ / 200 000 $ или флагам из админки), предупреждение об изменении условий, карточка юриста с WhatsApp, FAQ + JSON-LD FAQPage.
- Тексты черновые (пакет C за ChatGPT не начат) — см. `docs/CLAUDE-NOTES.md`. Цифры внесены в `docs/demo-data.md`.
- Ссылки: карточки программ на главной, «Гражданство» и «ВНЖ» в футере всех страниц, статьи 504–505, вопрос о ВНЖ на страницах районов.

---

## 2026-09-25 · Claude · Блог, новости, публикация, команда

- Владелец: «блог и новости тоже нужны и страница команды».
- `blog.html`: главная статья + сетка, фильтр по рубрикам (`?cat=`), JSON-LD Blog. `news.html`: лента новостей с источниками, фильтр по рубрикам, боковая колонка (курсы валют, статьи, Telegram).
- `post.html?id=`: один шаблон для статьи и новости. Автор со ссылкой на `team.html#id`, дата, «Актуально на» (поле `reviewed`), содержание по H2, источник для новостей, теги, «поделиться», блок автора, объекты по теме (`relatedProperties` / `relatedDistricts`), похожие публикации. JSON-LD Article/NewsArticle + BreadcrumbList. `?preview=1` показывает черновики.
- `team.html`: основатель, 5 экспертов с районами и специализацией (якоря `#expert1`, `#lawyer`…), правила работы, офис, лицензия (`#license`), статьи экспертов. JSON-LD RealEstateAgent.
- Тексты: `assets/posts.js` (7 статей, 7 новостей, демо). Биографии и специализация — в `TEAM` (`data.js`). Стили внутренних страниц объединены в `assets/pages.css` (бывший `districts.css`).
- Текст из админки выводится через `KHS.safeHtml` (убирает script, on*-атрибуты, javascript:-ссылки); краткое описание — через `DOMParser`, чтобы картинки из текста не выполняли код.
- Опубликованное в админке сразу появляется в блоге/новостях. Ссылки: меню «О компании» → `team.html`, «Журнал» → `blog.html`, футер, журнал и документы на главной, эксперт на странице объекта.
- Проверено в Chromium на 1280 и 390 px: 14 публикаций, блог, новости, команда без ошибок JS и горизонтальной прокрутки.

---

## 2026-09-25 · Claude · Прототипы онлайн на GitHub Pages

- По просьбе владельца («давай на git запустим») прототипы публикуются на https://restvip7-lang.github.io/realestate/.
- `.github/workflows/pages.yml`: после каждого слияния в `main` (если менялся `design/prototypes/`) копирует прототипы и добавляет во все страницы `noindex, nofollow`, потому что данные выдуманные. Можно запустить вручную: Actions → «Прототипы на GitHub Pages» → Run workflow.
- `design/prototypes/index.html` перенаправляет на `home-v2.html`.
- Нужно один раз включить Pages в настройках репозитория (Settings → Pages → Source: GitHub Actions) — делает владелец.

---

## 2026-09-25 · Claude · Страницы районов (пакет A, часть 1)

- Владелец выбрал следующим шагом страницы районов. Решение по заявкам: сначала письмом на e-mail, Telegram позже (записано в `docs/PLAN.md`).
- `design/prototypes/districts.html`: все 14 районов. Схема побережья (каждый район — ссылка), фильтры «У моря / В горах / Для жизни / Для аренды / Тихие / Доступные», сортировка, таблица сравнения, FAQ «Как выбрать район», SEO-текст, JSON-LD ItemList.
- `design/prototypes/district.html?d={slug}`: один шаблон на все районы. Факты (до центра, моря, аэропортов GZP и AYT, цена за м², число объектов), оценки 1–5, плюсы и минусы, инфраструктура, объекты района (продажа/аренда), цены по планировкам из каталога, приближенная схема, соседние районы, FAQ, SEO-текст, форма заявки с названием района. JSON-LD: BreadcrumbList, Place, FAQPage.
- Тексты районов и оценки — в `GUIDE` в `assets/data.js` (черновик, проверит команда). `DIST_IN` из каталога перенесён туда же (`d.in`). Стили — `assets/districts.css`.
- Ссылки: меню «Районы» на всех страницах → `districts.html`; футер и блок районов на главной, SEO-текст каталога и страница объекта ведут на гиды районов.
- Добавлены демо-объекты 1032–1036 (Махмутлар, Оба), чтобы в главных районах было что показать.
- Проверено в Chromium на 1280 и 390 px: 14 районов + общий список без ошибок JS и горизонтальной прокрутки.
- Дальше: пакет A, часть 2 (блог, новости, 404).

---

## 2026-09-25 · Claude · Заглушки заполнены демо-данными

- Владелец: «пока цен нет и адреса тоже и телефона… пусть всё будет заполнено не настоящими данными», «лицензия пусть будет все единицы».
- Вместо `[адрес офиса]`, `+90 5XX…`, `TTYB № [—]`, `[Условия агентства…]` и ссылок `#` у мессенджеров стоят выдуманные значения: телефон +90 500 111 11 11, TTYB № 1111111, адрес на Atatürk Cad. № 11, условия комиссии, награды. Затронуты `home-v2.html`, `home.html`, `catalog.html`, `property.html`.
- Все значения собраны в `KH.COMPANY` (`assets/data.js`) и в таблице `docs/demo-data.md` — по ней заменять на реальные.

---

## 2026-09-25 · Claude · Разбор проекта, исправления, предложения

- Владелец загрузил проект на GitHub (restvip7-lang/realestate) одним коммитом через браузер, прямо в `main`. Работа Claude идёт в ветке `claude/modest-hamilton-62z9bd`.
- Исправлено в прототипах: молчаливая ошибка сохранения при переполнении localStorage, бюджет в выбранной валюте (главная и каталог), число фото (`KH.gallery`), проданный/сданный объект (без формы просмотра, JSON-LD `SoldOut`), экранирование текста из админки (`KHS.esc`). Проверено в Chromium на 1280 и 390 px.
- Предложения по приоритету — в начале `docs/CLAUDE-NOTES.md`, новые задачи — в `docs/TASKS.md`. Главные: заявки в админке, калькулятор расходов, избранное и сравнение, семантическое ядро (просьба к ChatGPT).
- Дальше: владелец выбирает пункты, затем Claude берёт пакет A.

---

## 2026-09-25 · ChatGPT · Рекомендации Claude для главной v2

- По просьбе владельца подготовил docs/home-v2-brief.md на основе его скриншотов RestProperty: порядок блоков, текстовые схемы desktop/mobile, тексты hero, карточки и критерии проверки.
- Ссылка и статус предложения добавлены сверху docs/GPT-IDEAS.md. Это задание для обсуждения и следующего прототипа, не утверждённый редизайн.
- Код сайта не изменён. Следующий шаг — Claude читает задание и готовит home-v2.html после принятия задачи.

---

## 2026-09-25 · ChatGPT · Оценка дизайна главной

- По просьбе владельца посмотрел localhost:8765/home.html в браузере на 1440×1000 и 390×844. Рекомендации и границы проверки — в начале docs/GPT-IDEAS.md.
- Главные предложения: сократить hero и поднять мобильный поиск; усилить читаемость цен/характеристик; добавить реальное доверие и полезную карточку района. Для следующего демо нужен путь поиск → объект → заявка и работающее мобильное меню.
- Код прототипа не менял. Пустые секции на full-page screenshot до прокрутки вызваны reveal-анимацией, не трактовать как отсутствие контента.
- Работал в отдельном worktree gpt/design-review; основную папку с main не переключал. Дальше — обсуждение предложений с владельцем и Claude.

---

## 2026-09-25 · Claude · Конец дня: где остановились

- Готово и в `main`: главная v2, каталог с картой, страница объекта, админка (вход, объекты, новости), 14 районов по карте владельца, вымышленные имена команды.
- **Завтра (Claude):** пакет A из `docs/TASKS.md` — страница района + все районы, блог + статья, новости + новость, 404. Затем пакет B.
- **Можно ChatGPT:** пакет C — тексты «Гражданство», «ВНЖ», «Услуги» в `docs/content/`.
- Локальный просмотр: `python -m http.server 8765` в папке `design/prototypes`, затем http://localhost:8765/home-v2.html

---

## 2026-09-25 · Claude · Имена сотрудников

По просьбе владельца заглушки «[Имя Фамилия]» заменены вымышленными именами (`TEAM` в `data.js`): Эмре Йылдыз — основатель, Анна Соколова — эксперт, Мурат Кая — юрист, Елена Демир, Даниэль Мартен, Амина Арслан — эксперты. На сайте есть пометка «имена вымышлены». Выбор менеджера в админке теперь показывается на странице объекта.

---

## 2026-09-25 · Claude · Исправлены районы по карте владельца

Владелец указал на ошибку: «Тепе вообще в горах находится» и прислал карту районов с расстояниями.
- Эталон сохранён: `docs/img/alanya-districts-owner-map.webp`, справочник — `docs/districts.md`.
- `DISTRICTS` в `data.js`: 14 районов (добавлены Паялар, Демирташ, Газипаша), расстояния до центра по карте, Тепе и Джикджилли помечены `inland` (в горах), координаты Тепе из OSM (36.578, 31.994).
- Новая схема побережья `KH.COAST` (+ `KH.MOUNTAINS`) используется на главной, странице объекта, в админке и в старой v1 (v1 помечена как устаревшая).
- Объекты горных районов: до моря 1,5–5 км, на карте вокруг центра района, а не у берега. Расстояние до аэропорта — `KH.airportKm(d)`.
- **ChatGPT:** при текстах о районах сверяйся с `docs/districts.md`.

---

## 2026-09-25 · Claude · Прототип каталога с картой

- `design/prototypes/catalog.html`: фильтры сверху (Купить/Аренда, тип, район, комнаты, цена) + шторка «Фильтры» (до моря, площадь, этаж, ID, вид на море, мебель, новостройка, от собственника, видео, животные для аренды), сортировка, активные фильтры-чипы, «Показать ещё» + номера страниц, 3 вида (список+карта / список / карта), «Скачать PDF» (печать), SEO-текст и ссылки по районам/типам/подборкам внизу.
- Карта: Leaflet + OpenStreetMap (cdnjs), ценники на метках, кластеры, подсветка карточки ⇄ метки, всплывающая мини-карточка, «Искать при перемещении карты».
- H1, title, хлебные крошки и JSON-LD ItemList строятся из фильтров («Купить квартиры в Махмутларе»). Показан будущий чистый адрес (`/ru/sale/apartments/mahmutlar/`) и пометка «фильтры → noindex».
- Состояние хранится в URL (можно отправить ссылку, работает «Назад»).
- Данные: 52 дополнительных демо-объекта (генератор в `data.js`), координаты районов из OpenStreetMap (Nominatim), точки ставятся от берега с учётом «до моря».
- Главная: поиск ведёт в каталог с параметрами; ссылки «Каталог», районы, футер и хлебные крошки объекта — в каталог.
- Админка: координаты при редактировании больше не пересчитываются со схемы, только по клику.
- Проверено: 1280 и 390 px, фильтры, шторка (Escape, фокус), аренда, сброс, сортировка, «Показать ещё», поиск по области карты, мобильная карта, путь главная → каталог → объект.
- Замечание для тестов: во встроенном браузере при скрытой панели анимации Leaflet не завершаются, поэтому подгонка карты сделана без анимации (`animate:false`).

---

## 2026-09-25 · Claude · Фото вместо пустых блоков + вход в админку

По просьбе владельца: «не должно быть пустых блоков», «админку нужно запаролить и дать возможность восстанавливать пароль через почту».
- **Фото:** команда (основатель, эксперт, юрист + эксперты на страницах объектов через `KH.agentFor`), офис, основатель / лицензия / награды, обложки новостей. Все — стоковые снимки Unsplash, подписаны «фото для примера», заменить на реальные. Данные в `assets/data.js` (`TEAM`, `PHOTOS`).
- **Вход:** `admin/login.html` (при первом запуске создаёт учётную запись), `forgot.html`, `reset.html?token=`, `account.html` (смена пароля), `mailbox.html` (демо-почта вместо настоящих писем), `auth.js` (охрана страниц через `data-guard`, выход в боковом меню).
- Правила: пароль от 10 символов с буквой и цифрой; 5 ошибок → блок 60 сек; ответ «Забыли пароль?» одинаковый для любых адресов; ссылка одноразовая, 30 минут; после сброса все сессии завершаются; `next` принимает только локальные `*.html`.
- ⚠ Это имитация в браузере (хеш SHA-256 в localStorage), не защита. В рабочей версии — авторизация Payload + SMTP, описано в `docs/PLAN.md`.
- Проверено в браузере: весь сценарий от закрытой страницы до входа с новым паролем.
- Исправлено: атрибут `hidden` перебивался `display:grid`; в `proto.css` добавлено правило `[hidden]{display:none!important}`.

---

## 2026-09-25 · Claude · Главная v2, страница объекта, прототип админки

По заданию ChatGPT `docs/home-v2-brief.md` и просьбе владельца («не забудь админку для объектов продажи и аренды» и «новости ещё»).
- **Сайт:** `design/prototypes/home-v2.html` + `property.html`. Работают: поиск (Купить/Аренда, тип, район, комнаты, бюджет от–до, «Больше фильтров», поиск по ID, нулевой результат), подборки, 6 карточек → страница объекта, районы (фото + «Сравнить районы» со схемой), мобильное меню (Escape, фокус), валюты, избранное, формы с проверкой и честным «демо: не отправлено».
- **Админка:** `design/prototypes/admin/objects.html`, `object-edit.html` (переключатель Продажа/Аренда, 7 вкладок, фото перетаскиванием, 45 пунктов инфраструктуры, точка на схеме, RU/EN/TR, SEO со сниппетом, проверка обязательных полей), `posts.html`, `post-edit.html` (новость/статья, редактор, 3 языка, планирование, закрепление, источник).
- Связь через localStorage: опубликованное в админке сразу видно на сайте (объект — в поиске и на своей странице, новость — на главной; запланированная — нет).
- Общие файлы: `design/prototypes/assets/` (proto.css, site.js, data.js, admin.css). Новые прототипы (каталог, лендинг) делать на них.
- Проверено в браузере: 360 / 390 / 768 / 1280 / 1440 px без горизонтальной прокрутки; кнопка поиска в первом экране (390×844 → 580 px, 1440×900 → 684 px). Сценарии поиск → объект → просмотр, админка → публикация → сайт пройдены.
- Не проверено: Lighthouse, клавиатурная навигация по всей админке, EN/TR.
- `docs/design-system.md` → v0.2, `docs/PLAN.md` — поля аренды и новостей, роли.

---

## 2026-09-25 · Claude · Разбор restproperty.ru

- По вопросу владельца прошёл каталог, карточку, «Гражданство», «Проданные», «Партнёрам» на restproperty.ru. Результат в `docs/restproperty-audit.md` (что берём ➕, что делаем лучше ⚡, что не берём).
- В `docs/PLAN.md` добавлен раздел «Дополнения после разбора restproperty.ru» (новые поля объектов, фильтры, разделы). В `docs/TASKS.md` добавлены задачи.
- Их слабые места, которые делаем лучше: нет карты в карточке, нет разметки RealEstateListing, не найден hreflang, перегруженное меню.

---

## 2026-09-25 · Claude · Этап 1: дизайн-система и прототип главной

- `docs/design-system.md`: концепция «Навигатор по побережью», цвета, шрифты (Unbounded + Onest + JetBrains Mono вместо Manrope + Inter), сетка, компоненты, анимации.
- `design/prototypes/home.html`: кликабельный прототип главной (один HTML-файл, без сборки). Работают валюты € $ ₺ ₽, избранное, интерактивная карта районов с ценами за м², форма заявки (демо), мобильная липкая панель WhatsApp / Telegram / звонок. Проверено на 1280 px и 375 px, горизонтальной прокрутки нет.
- Все цены, цифры, отзывы и эксперт в прототипе — **демо-заглушки**. Фото с Unsplash (только для прототипа).
- Открыть: файл в браузере, или `python -m http.server` в папке `design/prototypes`.
- **Ждём:** отзыв владельца по стилю. Потом прототипы каталога, карточки объекта и лендинга (задачи свободны в `docs/TASKS.md`).

---

## 2026-09-25 · ChatGPT · Синхронизация с main

- По просьбе владельца закоммитил исследование (67c614e) и влил main в gpt/brand-research без конфликтов (1d40801).
- Прочитал docs/CLAUDE-NOTES.md, обновлённый docs/brand.md и правила AGENTS.md. В заметках Claude зафиксирован выбор владельца: **Kleo Homes**. Принял это как актуальное решение; старые варианты в GPT-IDEAS.md остаются историей.
- Общая папка остаётся на gpt/brand-research; файлы Claude теперь доступны. Новую разработку не начинал.
- Ответ Claude записан сверху docs/GPT-IDEAS.md. Следующий этап — дизайн по общему плану; покупка домена и проверки бренда ещё не завершены.

---

## 2026-09-25 · ChatGPT · Независимая проверка названий

- По просьбе владельца создал `docs/GPT-IDEAS.md` для обмена идеями с Claude. Там варианты, источники, результаты проверки доменов, рекомендации по рекламе и SEO.
- Предлагаю Alanyora; Kleo Homes оставлен альтернативой. Ничего не утверждено и не куплено.
- Важно: **Alanya Key может путаться с действующим агентством Key Alanya (keyalanya.com) в том же городе**. Рекомендую снять его с приоритетного выбора, несмотря на доступный .com.
- Vercel подтвердил доступность alanyora.com, alanyvia.com, alvessahomes.com, kleohomes.com и alanyakey.com на момент запроса. .com.tr не вернулись в ответе — статус неизвестен. Товарные знаки и ники не проверены.
- Рабочая ветка `gpt/brand-research`; docs/brand.md Claude не изменял. Помните: ветки в одной папке не изолируют работу; для параллельной разработки нужны отдельные worktree.
- Дальше: выбрать финалистов, проверить товарные знаки, ники и турецкие домены, затем утвердить название.

---

## 2026-09-25 · Claude · Варианты названия

- Создал `docs/brand.md`: требования к названию, шорт-лист из 6 вариантов со свободными .com, рекомендация.
- Главные кандидаты: **Alanya Key** (alanyakey.com) и **Kleo Homes** (kleohomes.com). Решение за владельцем.
- Пока название не выбрано, в коде используем переменную `SITE_NAME`.
- **Дальше:** владелец выбирает название → дизайн-система и прототип главной (этап 1). Семантическое ядро свободно, `[gpt]` может брать.

---

## 2026-09-25 · Claude · Этап 0 начат

**Что сделано**
- Изучил сайт-образец restproperty.ru: главную, меню, фильтры, карточку объекта. Структуру и наполнение берём за основу, дизайн делаем свой.
- С владельцем согласовали:
  - языки: **RU (основной), EN, TR**;
  - стек: **Next.js 15 + Payload CMS 3 + PostgreSQL + Tailwind + next-intl**;
  - стиль дизайна: **современный технологичный** (контрастный, строгий, акцент на поиске, карте и фильтрах);
  - бренда, домена и базы объектов **пока нет**, всё делаем с нуля.
- Создал git-репозиторий (ветка `main`) и `.gitignore`.
- Создал файлы:
  - `AGENTS.md` и `CLAUDE.md`: общие правила работы (содержимое одинаковое);
  - `docs/PLAN.md`: утверждённый план (URL-структура, модель данных, SEO, реклама, этапы);
  - `docs/TASKS.md`: чек-лист задач с метками `[claude]` / `[gpt]` / `[human]`;
  - `docs/brief.md`: вопросы к агентству (заполняет владелец).

**Ключевые договорённости**
- Каждый агент работает в своей ветке (`claude/*` или `gpt/*`). Перед тем как взять задачу, впиши метку в `docs/TASKS.md`.
- Все решения записываем в `docs/`.
- Цвета и шрифты берутся только из дизайн-системы (будет в `docs/design-system.md`).

**Что дальше (свободно, можно брать)**
- Варианты названия агентства и проверка доменов → `docs/brand.md`.
- Семантическое ядро RU/EN/TR (Аланья: купить квартиру, виллы, районы, гражданство, ВНЖ) → `docs/seo-keywords.md`.
- Дизайн-система и HTML-прототипы (этап 1).

**Ждём от владельца:** ответы в `docs/brief.md`.
